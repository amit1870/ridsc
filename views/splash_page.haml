!!! 5
%html

  %head
    %meta(charset="utf-8")
    %title Welcome to RIDS

    %link(href="/static/css/bootstrap.css", rel="stylesheet")
    %link(href="/static/css/rids.css", rel="stylesheet")

  %body.splash

    .container
      .row
        .span12
          .page-header.text-center
            %h1 Welcome to RIDS

      .row
        .span12.text-center
          - if ip_lookup:
            %p.lead To manage the content on this screen, just point your browser to:

            .well.lead
              %a(href="${url}") ${url}
          - else:
            %p.lead Unable to resolve management IP.

      %hr

      .row
        .span12.text-center
          Brought to you by <br />
          %a(href="http://rigvee.com")
            %img(src="/static/img/rigvee.png")
